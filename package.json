{
  "name": "fable-paint",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "type": "module",
  "scripts": {
    "build": "vite build && node scripts/build.mjs",
    "ci": "npm test && npm run build",
    "start": "node serve.mjs 8000",
    "serve": "node serve.mjs 8000",
    "test": "npm run test:wasm && npm run test:blur && npm run test:puppet && npm run test:stroke && npm run test:raster-worker",
    "test:blur": "node js/blur_brush_test.mjs",
    "test:raster-worker": "node js/raster_worker_test.mjs",
    "test:wasm": "node wasm/test.mjs",
    "test:puppet": "node js/puppet_test.mjs",
    "test:stroke": "node js/stroke_straight_test.mjs"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  },
  "dependencies": {
    "opentype.js": "^2.0.0",
    "vtracer-webapp": "^0.4.0"
  }
}
